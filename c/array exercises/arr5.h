#include <stdio.h>
#define ARR_MIN_SIZE 1

/*
-This function sort array of "1's" and "0's"
-It get as an input array of "1's" and "0's", and size of the array
-If it is array with 1 organ, the function will print it, else if it an empty array(size of 0) the function will print matching massage, elsewise the function print the new sorted array.
*/

int sortArray(int _arr[], _size);
