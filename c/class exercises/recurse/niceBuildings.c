#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/*Move disc #n from # to #*/
void Hanoi(int_n, char _from, char _to, char _via)
{	
	if(_n < 0)
	{
		return
	}
}

int main(void)
{
	int arr[] = {1,3,5,7};
	int average, i = 0;
	int len = sizeof(arr)/sizeof(arr[0]);
	while(i < len)
	{
	printf("%d ",arr[i]);
	i++;
	}
	average = Average(arr, len);
	printf("The Average of the array is: %d\n", average);
}
