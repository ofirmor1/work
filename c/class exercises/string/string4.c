#include <stdio.h>
#include <string.h>

char* deleteMatches(char *str1, char *str2)
{
	int indexStr1,indexStr2;
	int sizeStr1 = strleng(str1), sizeStr2 = strlen(str2);;
	UnifiedStr = strcat(str1, str2);
	sizeUnifiedStr = strlen(UnifiedStr);
	for(indexStr1 = 0,indexStr2 = UnifiedStr+sizeStr1; indexStr1 < sizeStr; index++)
	{
		if(
	}
}
